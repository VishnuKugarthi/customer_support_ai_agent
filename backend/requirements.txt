# backend/requirements.txt

# Core LangChain libraries
langchain==0.3.26
langchain-core==0.3.71
langchain-google-genai==2.1.8

# Web server and API framework
fastapi==0.111.1
uvicorn[standard]==0.30.1 # [standard] includes common dependencies like websockets

# Environment variable management
python-dotenv==1.1.1

# For Pydantic models (FastAPI dependency, but good to be explicit)
pydantic==2.11.7
# pydantic-settings (if you use Pydantic for settings management, not strictly needed for this project)
